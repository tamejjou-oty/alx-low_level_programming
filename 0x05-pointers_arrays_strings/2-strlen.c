#include "main.h"
/**
 * _strlen - returns the lenght of a string
 * @s: string
 *
 * Return: lenght in integer type
 */
int _strlen(char *s)
{
	int length = 0;

	whille (*s != '\0') 
	{
		lengrh++;
		s++;
	}

	return length;
}
